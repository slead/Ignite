!!!
%html
  %head
    %meta{'charset' => 'utf-8'}
    %meta{'content' => 'text/html'}
  %body
    %p= "Hi #{@user.name}, a new video has been added to IgniteTalks.io:"
    %h3=link_to @video.title, @video
    %p= "Speaker: #{@video.speaker_name}. Filmed at: #{@video.event.name}. Added by: #{@video.user.name}"
    =image_tag  "http://img.youtube.com/vi/#{@video.uid}/hqdefault.jpg"
    %p= "YouTube URL: #{@video.url}"
    %p= @video.description
    %ul
      %li
        = number_with_delimiter(@video.views, :delimiter => ',')
        views
      %li
        = number_with_delimiter(@video.likes, :delimiter => ',')
        likes
      %li
        = number_with_delimiter(@video.dislikes, :delimiter => ',')
        dislikes
    %p Tags:
    %ul
      - @video.tags.each do |tag|
        %li= tag.name
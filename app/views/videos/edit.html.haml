.container.top-150
  .row
    .col-md-8.col-md-offset-2
      %h3.left-15 Edit video
      .embed-container
        =embed(@video.uid)

  .row 
    .col-md-8.col-md-offset-2
      .panel
        .panel-body
          = render 'form'

/ Disable the YouTube retrieve button in Edit mode
:javascript
  $(document).ready(function() {
    $(".details").show();
    $("#btnYouTubeRetrieve").addClass("disabled");
  });
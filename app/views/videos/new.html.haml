.container.top-150
  .row
    .col-md-8.col-md-offset-2
      %h2 Add a new Ignite video
      .well
        %h4 Notes on adding Ignite videos
        %p
          Videos must be stored on YouTube in HD (high definition) format. Videos are associated with an Ignite (eg Ignite Seattle) - if you don't see your Ignite listed, you need to
          =link_to "add your Ignite", new_event_path
          first, then return to this page.

  .row 
    .col-md-8.col-md-offset-2
      .panel
        .panel-body
          = render 'form'
        / -if current_user.admin?
        /   %button.btn
        /     = link_to "Back to Admin page", admin_path

/ Enable the YouTube retrieve button when there's a URL to test
:javascript
  $(document).ready(function() {
    $('[data-toggle="tooltip"]').tooltip();
    $("#btnYouTubeRetrieve").addClass("disabled");
    if ($("#video_url").val() == '') {
      $(".details").hide();
    } else {
      $(".details").show();
    }
  });
.container-fluid
  .row
    .col-md-8.col-md-offset-2
      %h2.center Featured playlists
      %p.center
        IgniteTalks.io contains Ignite videos from around the world.
        %span.hidden-sm.hidden-xs
          = link_to "Tell us", "https://docs.google.com/forms/d/1NrIlpRb_IvBl1zGw0LuVhipUxeVJE1r28UUglcFXYjQ/viewform", :target => "_blank"
          about a great Ignite video!
  %hr
  .row
    .col-md-4.col-sm-12.col-xs-12
  /     = form_tag playlists_path, class: "form-inline", method: :get do
  /       .input-group
  /         = text_field_tag :query, params[:query], class: "form-control", id: "search", autocomplete: "off"
  /         .input-group-btn
  /           - if params[:query].present?
  /             = link_to playlists_path, class: "btn btn-default" do
  /               %i.fa.fa-times
  /           = submit_tag "Search", class: "btn btn-default"
  /   .col-md-8.right.hidden-sm.hidden-xs
  /     =link_to playlists_path do
  /       %span.btn.btn-default.tags#clear-filter
  /         %i.fa.fa-times
  /         clear
  /     =link_to playlists_path(sort: "views") do
  /       %span.btn.btn-default.tags{:id => "tag_views"}
  /         %i.fa.fa-youtube-play
  /         = "views"
  /     =link_to playlists_path(sort: "likes") do
  /       %span.btn.btn-default.tags{:id => "tag_likes"}
  /         %i.fa.fa-thumbs-o-up
  /         = "likes"
  /     - @tags.each do |tag|
  /       =link_to playlists_path(tag: tag.name) do
  /         %span.btn.btn-default.tags{:id => "tag_" + tag.name}= tag.name.titleize
  .row.top-50
    -@playlists.each do |playlist|
      .row.top-15
        .col-md-12
          %h4= link_to playlist.name, playlist
        -playlist.videos.each_slice(4) do |vids|
          .row.top-15
          -vids.each do |video|
            .col-md-3.video_thumbnail
              %span= thumbnail(video, 'video_rating_small', 60)

  .row.paginate
    = will_paginate @playlists, renderer: BootstrapPagination::Rails
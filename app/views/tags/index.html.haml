.container.top-150
  .row 
    .col-md-8.col-md-offset-2
      %h1.center Find videos by tags

  - @tags.each_slice(4) do |tags|
    .row.top-15
      -tags.each do |tag|
        .col-md-3
          %strong
            = link_to tag.name.camelize, tag
          %br
          =pluralize tag.videos.count, "video"
          - if can? :update, tag
            = link_to edit_tag_path(tag), class: "light btn-edit" do
              %i.fa.fa-pencil-square-o
            = link_to tag_path(tag), method: :delete, class: "light btn-delete", data: {confirm: "Are you sure you want to delete this tag?"} do
              %i.fa.fa-times
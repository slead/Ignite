.container
  .row
    .col-md-12
      %h1 Event index
      -@events.each do |event|
        .row
          %p= event.name
          %p= "#{event.city}, #{event.state}, #{event.country}"
          %p= link_to "Ignite" + event.name, event.url
        %hr